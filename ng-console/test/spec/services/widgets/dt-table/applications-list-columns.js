'use strict';

describe('Service: widgets/dtTable/applicationsListColumns', function () {

  // load the service's module
  beforeEach(module('dtConsoleApp'));

  // instantiate service
  var widgets/dtTable/applicationsListColumns;
  beforeEach(inject(function (_widgets/dtTable/applicationsListColumns_) {
    widgets/dtTable/applicationsListColumns = _widgets/dtTable/applicationsListColumns_;
  }));

  it('should do something', function () {
    expect(!!widgets/dtTable/applicationsListColumns).toBe(true);
  });

});
