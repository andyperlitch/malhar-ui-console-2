'use strict';

describe('Service: overviewFields/clusterMetricsOverviewFields', function () {

  // load the service's module
  beforeEach(module('dtConsoleApp'));

  // instantiate service
  var overviewFields/clusterMetricsOverviewFields;
  beforeEach(inject(function (_overviewFields/clusterMetricsOverviewFields_) {
    overviewFields/clusterMetricsOverviewFields = _overviewFields/clusterMetricsOverviewFields_;
  }));

  it('should do something', function () {
    expect(!!overviewFields/clusterMetricsOverviewFields).toBe(true);
  });

});
