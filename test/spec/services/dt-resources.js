'use strict';

describe('Resource: ClusterMetrics', function () {

  // load the service's module
  beforeEach(module('dtConsoleApp'));

  // instantiate service
  var ClusterMetrics;
  beforeEach(inject(function (_ClusterMetrics_) {
    ClusterMetrics = _ClusterMetrics_;
  }));

  

});
